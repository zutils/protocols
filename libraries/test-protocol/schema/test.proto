syntax = "proto2";

message Test {
  required string name = 1;
  required string data = 2;
}

message Empty {}

service ClientRPC {
    rpc receive_test(Test) returns (Empty);
}

service PublicRPC{}
service ServerRPC{}